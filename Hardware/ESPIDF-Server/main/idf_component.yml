## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: '>=5.3'
  # # Put list of dependencies here
  # # For components maintained by Espressif:
  # component: "~1.0.0"
  # # For 3rd party components:
  # username/component: ">=1.0.0,<2.0.0"
  # username2/component2:
  #   version: "~1.0.0"
  #   # For transient dependencies `styles` flag can be set.
  #   # `styles` flag doesn't have an effect dependencies of the `main` component.
  #   # All dependencies of `main` are styles by default.
  #   styles: true
  espressif/qrcode: '*'
  espressif/arduino-esp32: ^3.2.0
